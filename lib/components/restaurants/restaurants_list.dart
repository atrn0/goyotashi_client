import 'package:flutter/cupertino.dart';

class RestaurantsList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Text('restaurant') // TODO: replace with restaurant widget
      ],
    );
  }
}
